{
  "name": "getNewsByQuery",
  "description": "Get news headlines by query",
  "parameters": {
    "type": "object",
    "properties": {
      "q": {
        "type": "string",
        "description": "Keywords or phrases to search for in the article title and body."
      },
      "category": {
        "type": "string",
        "description": "the category of the news. Default is general",
        "enum": [
          "business",
          "entertainment",
          "general",
          "health",
          "science",
          "sports",
          "technology"
        ]
      },
      "pageSize": {
        "type": "number",
        "description": "The number of results to return per page (request). 5 is the default, 100 is the maximum."
      },
      "page": {
        "type": "number",
        "description": "Use this to page through the results."
      },
      "sortBy": {
        "type": "string",
        "description": "relevancy, popularity, publishedAt"
      }
    },
    "required": [
      "q",
      "category"
    ]
  }
}